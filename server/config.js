/* ----------------------- configuration for database ----------------------- */
const config = {
    db:{
        host: 'localhost',
        user: 'root',
        password : '',
        database: 'game'
    },
    listPerPage:10
};

module.exports = config;